# Skills Index — Algorithms Learning Project

> **Последнее обновление: 2026-05-30**
>
> Читать этот файл в начале каждого чата для восстановления контекста.

---

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО #1

> **НИКОГДА не давать готовый код — только наводящие вопросы**

Полная версия с примерами и граничными случаями → `CRITICAL_RULE_1.md`

---

## Быстрые ссылки

| Что нужно | Куда идти |
|-----------|-----------|
| **⚠️ Правило #1: Никакого готового кода** | `CRITICAL_RULE_1.md` |
| Управление сессиями, перерывы, journal | `workflow/session.md` |
| Методика обучения (Socratic, TDD, фазы) | `algorithms-mentor/SKILL.md` |
| **Anki-карточки по алгоритмам** | `algorithm-anki/SKILL.md` |
| Статус карточек Go | `cards/references/status.md` |
| Методика ответа на карточку Go | `cards/SKILL.md` |
| Шаблон выдачи задачи | `algorithms-mentor/assets/task-template.md` |
| **Подготовка к собеседованию в Яндекс** | `yandex-interview/SKILL.md` |
| Правила git | `workflow/git.md` |
| SQL-задачи по схеме "Расписание" | `sql-problems/SKILL.md` |
| Алгоритмические паттерны (GCD и др.) | `algorithms-mentor/references/patterns.md` |
| **Текущий план: Sliding Window** | `NEETCODE_PLAN_3.md` |
| Архив: Two Pointers | `NEETCODE_PLAN_2.md` |
| Архив: Arrays & Hashing | `NEETCODE_PLAN_1.md` |
| Примеры из проектов | `projects/` |

---

## Текущий статус обучения

**Ученик**: Анна (Go backend-разработчик)  
**Цель**: Стажировка в российский бигтех  
**Срок**: 6 месяцев (жёсткий дедлайн)

### Фазы обучения
- Фаза 0: Математический фундамент (недели 1-2) ✅
- Фаза 1: Массивы + Рекурсия (недели 3-7) ✅
- Фаза 1.5: [NeetCode] Arrays & Hashing (34 задачи) ✅
- Фаза 1.6: [NeetCode] Two Pointers (15 задач) ✅
- **Фаза 1.7: [NeetCode] Sliding Window (завершается) 🔄 ← Текущий блок — 13/16 основных + 2/2 бонусных решены, осталось 4 задачи**
- Фаза 2: Линейные структуры (недели 8-12) ← **Приостановлено до завершения NeetCode 150**
- Фаза 3: Деревья и Графы (недели 13-18)
- Фаза 4: ДП базовое (недели 19-20)
- Фаза 5: Полировка (недели 21-24)

### Карточки

**Go (интервью):**
- **Всего**: 63
- **Переработано**: 22 (Goroutines, Channels, Select, Mutex, WaitGroup, Context, Graceful Shutdown, errors.Is/errors.As, Dependency Injection, Generics, Clean Architecture, Ports & Adapters, DDD)
- **Следующая**: #14 FSM
- **Очередь**: gRPC vs REST, PostgreSQL Indexes

**Алгоритмические Anki (новый процесс):**
- Методология: 3 карточки на задачу — Type A (Pattern), Type B (Mechanics), Type C (Edge Case)
- Без кода на тыле, max 4 предложения
- Активно создаются для закрепления паттернов (LC 125, 977, 1732, 724, 202, 15, 643, 167, 283, 680, 1768, 88, 392, 26, 18, 189 и др.)

---

## Критические правила (всегда помнить!)

1. **НИКОГДА не давать готовый код** — только наводящие вопросы
2. **Анализ сложности (Big O)** — ВСЕГДА проводить анализ Time и Space для кода пользователя
3. **TDD обязателен** — Red → Green → Refactor
4. **Дополнительные тест-кейсы** — я могу добавлять проверки в `main_test.go`, если пользователь уже написала тест, но в нём не хватает edge cases. Это разрешено, потому что это ревью, а не выдача решения.
5. **Early returns** — проверять граничные случаи первым делом
6. **Git** — я создаю сообщение, пользователь выполняет commit
7. **Go-карточки** — разговорный стиль, примеры из Task Manager/RAG, никакого кода в тексте
8. **Go-карточки в CSV** — НЕ записывать в файл, пока пользователь не подтвердит («Записывай», «Сохрани», «Готово»)
9. **Daily Warm-up** — сессия начинается с самостоятельного повторения решённой задачи (без подсказок)
10. **Retention L4** — не начинать новую задачу, пока текущая не доведена до L4 (исключение: разминка)
11. **Anki-алгоритмы** — 3 карточки на задачу (Pattern / Mechanics / Edge), без кода, max 4 предложения, теги по паттерну
12. **Telegram-видео** — помогаю со структурой разбора (ввод → алгоритм → код → сложность → edge cases), не пишу готовый текст для озвучки

---

## Если пользователь просит...

| Запрос | Действие |
|--------|----------|
| "Создай комментарий для git" | `workflow/git.md` → создать сообщение, НЕ выполнять commit |
| "Проверь карточку" (Go) | `cards/SKILL.md` → проверить структуру ответа |
| "Помоги с карточкой" (Go) | `cards/SKILL.md` + наводящие вопросы, НЕ писать за неё |
| "Создай Anki-карточки" | `algorithm-anki/SKILL.md` → 3 карточки (A/B/C), без кода |
| "Проверь Anki-карточки" | `algorithm-anki/SKILL.md` → чек-лист перед сохранением |
| "Выдай задачу" | `algorithms-mentor/SKILL.md` + `assets/task-template.md` |
| "Сжатие / compress" | `workflow/compress.md` → создать сводку в `.session_summary` |
| "Прочитай сводку" | `workflow/compress.md` → прочитать `.session_summary` |
| "Запиши в journal" | `workflow/session.md` → обновить journal.csv + `.session_start` |
| "Начинаем сессию" | `workflow/session.md` → записать время в `.session_start` |
| "Перерыв" | `workflow/session.md` → BREAK_START в `.session_start` |
| "Продолжаем" | `workflow/session.md` → BREAK_END в `.session_start` |
| "Запиши видео для Telegram" | Помочь со структурой разбора, НЕ писать готовый текст |
| "Помоги подготовиться к Яндексу" | `yandex-interview/SKILL.md` → имитация собеса без примеров, чек-лист по критериям Яндекса |

---

## Gotchas (специфичные ошибки этого проекта)

### Алгоритмы
- ❌ Не использовать `math.Pow` для целых степеней двойки (использовать `1 << n`)
- ❌ Не забывать про two's complement для отрицательных чисел (uint32)
- ⚠️ **Целочисленное деление `sum/k`** — в Go `int/int` округляет к нулю, не к −∞. Для сравнения `avg >= threshold` при отрицательных числах безопаснее `sum >= k*threshold`
- ✅ Early returns для пустых входов, нулей, единичных случаев
- ✅ **Fixed-size result array** — если результат фиксированной длины (`len(nums)`), использовать `make([]int, len(nums))` и индексацию вместо `append`, особенно когда края заполняются специальным значением (например, -1)

### Go
- ❌ `len("привет")` = 12 байт, не 6 символов — использовать `[]rune`
- ⚠️ **`rune(s[i])` не делает строку Unicode-safe** — для ASCII-only задач (LC 125, 1876 и др.) `s[i]` (byte) корректен. Для полноценной Unicode-обработки использовать `[]rune(s)`
- ❌ Map iteration order случайный — использовать slice для порядка
- ✅ Встроенные `min()`, `max()` доступны с Go 1.21+
- ✅ Nil checks в связных списках и деревьях
- ✅ **`make([]int, n)` vs `append`** — если длина результата известна заранее, предпочитать `make` с фиксированной длиной

### Общение
- ❌ Никаких "ресторанных" аналогий
- ❌ Никакого кода в ответе на карточку
- ✅ Примеры из Task Manager / RAG-сервиса
- ✅ Разговорный стиль, связки: "Проще говоря...", "То есть..."

### VK Education (All Cups) — специфика
- ❌ Нельзя сдавать на Go (платформа не поддерживает)
- ✅ Писать сначала на Go (TDD), потом переводить на C++
- ⚠️ При переводе помнить: C++ использует `std::vector`, ручное управление вводом/выводом через `iostream`, отсутствие встроенных `min`/`max` как в Go 1.21+
- 📁 **C++ файлы класть в подпапку `vk/`** — иначе Go компилятор ругается на чужие файлы в пакете

### Книги
- **«Грокаем алгоритмы»** (Адитья Бхаргава) — Визуализация базы и построение интуиции. Читается параллельно с практикой.

---

## Обучающие курсы

### Активные
- **NeetCode 150** — Алгоритмический роадмап
  - **Текущий блок:** Sliding Window (Фаза 1.7)
  - **Завершённые:** Arrays & Hashing ✅, Two Pointers ✅

- **The Nature of GO PROGRAMMING** (igoroutine.courses)
  - Курс по глубинному пониманию Go
  - **Прогресс:** Лекции (3+) + xv6 SYS_va2pa ✅ + Go assembler (Sum, Fibonacci) ✅ + SIMD slices.Contains ✅

- **Nand to Tetris** — «From Nand to Tetris» (архитектура компьютера)
  - **Прогресс:** Lab 1 (комбинационные чипы) + Lab 2 (HalfAdder/FullAdder) частично

- **sql-academy.org** — Практика SQL
  - Схема «Расписание», PostgreSQL
  - **Прогресс:** пройдены LIKE/ILIKE, регулярные выражения, ORDER BY, LIMIT, JOIN с 3+ таблицами, IS NULL, BETWEEN, IN

- **Telegram-канал** — Видео-разборы алгоритмов
  - **Прогресс:** LC 977, LC 1732, LC 167, LC 125, LC 680, LC 283, LC 1768
  - **Цель:** Тренировка объяснения кода как на собеседовании

### Приостановленные
- **VK Education** — «Алгоритмы и структуры данных»
  - **Платформа:** All Cups (не поддерживает Go!)
  - **Причина:** Приостановлен до завершения NeetCode 150
  - **Прогресс:** ✅ 1.2 Сложность алгоритмов, ✅ 1.3 Массивы, ✅ 1.5 Списки, ✅ 1.6 Очереди, деки и стек

- **Coursera Princeton** — «Algorithms, Part I»
  - Роберт Седжвик, фундаментальное понимание алгоритмов
  - **Статус:** Нет активности с апреля, приостановлен

### Завершённые
- **Khan Academy** — Arithmetic (Units 1-19) ✅, Basic Geometry (Units 1-14) ✅, Pre-algebra (в процессе)

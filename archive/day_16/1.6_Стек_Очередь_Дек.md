Стек, очередь и дек Алгоритмы и структуры данных Михаил Павлов Что
будет на занятии Что такое стек

Как реализовать стек

Основные операции над стеком

Что такое очередь

Как реализовать очередь

Основные операции над очередью

Что такое дек

Как реализовать дек Стек Особенности Работает по принципу LIFO (last in,
first out) --- последний пришёл, первый вышел Добавлять и удалять
элементы можно только с одного конца Типичный пример --- стопка бумаг

                     Заберут 
      Теперь заберут
                     первым         первым вот этот



                                     А этот —
                                     последним

Пустой стек Положили Положили ещё первый элемент два в стек Где
применяется История браузера: каждый URL-адрес помещается в стек, при
нажатии кнопки «Назад» предыдущий URL-адрес извлекается из стека

Сtrl + Z в текстовом редакторе

Вызовы функций и рекурсия: при вызове функции текущее состояние
программы помещается в стек. Когда функция заканчивает выполнение,
состояние извлекается из стека, чтобы возобновить выполнение предыдущей
функции

Оператор defer в Golang: defer добавляет вызов функции, которая указана
после него в стеке приложения Основные операции Вставка (Push, enqueue)
--- добавить элемент в вершину стека Выборка (Pop, dequeue) --- извлечь
элемент. Сколько бы элементов ни было добавлено, всегда будет
возвращаться последний

                    push                  pop

Сложность операций --- всегда O(1) Реализация стека на списке Одна из
возможных реализаций Достаточно односвязного списка

Push будет писать данные, как это делал append_front

Pop будет возвращать head. Главное, не забывать переписывать указатели
Вставка (push) class Stack() {

     constructor() {


            this.top = null


     }

                                                          new_node             top
     function push(data) {


           # создать новый узел и добавить в него                     next

           # новое значение data


           new_node = Node(data)


           # если ранее стек был пуст, первый элемент
      top
           # и будет головой (head)


           if not this.top {


                this.top = new_node


                return


           }


           # если стек не пуст, то установить head

                                                             class Node() {

           # в качестве параметра next для нового узла

                                                                  constructor(data) {

           new_node.next = this.top


           # записать в head новый узел
                               this.data = data


           this.top = new_node
                                        this.next = null

      }
                                                          }

} }

Выборка (pop) Проверить, что стек содержит хотя бы одну вершину

Если стек пуст, вернуть любое значение, по которому будет определяться
это состояние

Если стек не пуст и в нём хранится больше одного значения, переписать
значение вершины на следующий элемент

Если стек пуст, установить вершину  в значение None Выборка (pop)

function pop() {

     # проверить, что стек содержит хотя бы вершину

     if not this.top {

            # можно возвращать -1

                                                                   top
            return null

     }
                                                 pop

     top = this.top

     if this.top.next != null {

           # переписать значение вершины 
                                                                                   top
           # на следующий элемент

           this.top = this.top.next

     } else {

           # если стек пуст, то установить вершину
           # в значение None

           # можно указать любое другое значение,
           # которое было бы удобно в реализации
         class Node() {


           this.top = null
                                   constructor (data) {

     }

                                                                   this.data = data


     # вернуть значение, хранящиеся на вершине стека
              this.next = null

     return top.data
                                         }

} } Реализация стека на массиве 1 Если неизвестно, сколько данных будет
приходить в стек, лучше всего использовать саморасширяющийся массив

2 Вставка в конец, чтобы обеспечить амортизационную сложность O(1) class
Stack {

function initialize(size) { Реализация стека  на простом this.stack =
Array\[size\]

           this.top = null

}

                                                          массиве

\# Проверить, пуст ли стек

function isEmpty() {

          return this.top == null

}

\# Проверить, полон ли стек

function isFull(size) {

          return this.top == size - 1

    }

\# Добавить элемент в стек

function push(element, size) {

          if this.isFull(size) {

                print "Стек полон"

          } else {

                                                                            Top
                 this.top = this.top +1

                 this.stack[this.top] = element
          }
    }

    # Удалить элемент из стека

    function pop() {

           if this.isEmpty() {

                print "Стек пуст"

                                                            H   e   l   l   o
           } else {

                  element = this.stack[this.top]

                  this.top = this.top - 1

                  return element
          }
      }

      #Получить верхний элемента стек без его удаления

      function top() {

             if this.isEmpty() {

                 print "Стек пуст"

             } else {

                   return this.stack[this.top]

             }

       }

} Очередь Особенности Работает по принципу FIFO (first in, first out)
--- первый пришёл, первый вышел Элемент, который был положен в очередь
первым, при запросе будет первым удалён

Типичный пример --- очередь к кассе Основные операции Вставка (Push,
enqueue) --- добавить элемент в конец очереди Выборка (Pop, dequeue) ---
извлечь элемент. Это можно сделать только из начала очереди

                         push

Сложность операций --- всегда O(1)

                                          pop

Реализация очереди на примере массива Head

          H
              E
                  L

                   L

                  O
                         Tail

Кольцевой буфер

(циклический массив)

head tail

8

                           Head

head tail

                   H
                       E

                           L

                           L

                           O
                                  Tail

Кольцевой буфер

(циклический массив) Head

                           H

                               E

      head tail
                                   L


         8
                                   L


                                   O
      head tail                           Tail




                  3   11

pop Кольцевой буфер

(циклический массив) Head

                           H
         3     11

pop E

                                   L



                                   L


                                   O
                                          Tail
         3     11    9

pop

        head        tail

Кольцевой буфер

(циклический массив) Head

                       3       11     9     H

pop E

                                                    L
                      head           tail

                                                    L


      Элементы сдвинуты к нулевой                   O
      ячейке.
                                             Tail




       3         11    9




      head                    tail

Кольцевой буфер

(циклический массив) Head

                         3       11        9     H

pop E

                                                         L
                       head               tail

                                                         L



      Сдвигаем все элементы к нулевой ячейке.            O
                                                                Tail
      Тогда сложность стремится к O(n)





        3       11       9




      head                       tail

Кольцевой буфер

(циклический массив) Head

                     3      11     9     H

pop

                                             E


                    head          tail           L



                                                 L
                     3      11     9

                                                 O
                                                        Tail
      tail          head



      8                     11     9




             tail          head

Реализация очереди на саморасширяющемся массиве При чтении проверять
наличие элементов в очереди --- и если они есть, возвращать их и двигать
head При добавлении устанавливать элемент на индекс tail и двигать tail
на +1 к концу массива При заполнении копировать все элементы в новый, но
индексы переписывать с тем учётом, что head должен указывать на нулевую
ячейку

                 3      11      9

               head            tail

3 11 9

head tail Дек Особенности Дек --- (deque, double ended queue)
двусторонняя очередь Добавлять и извлекать элементы можно  с любой
стороны, но в порядке очереди

                          i l l g
                               s ...
                       s kd i n
                    IT a
                      lo

Основные операции push_front --- вставить элемент в начало очереди. То
же самое, что push в очереди и стеке push_back --- вставить элемент в
конец очереди pop_front --- извлечь из начала очереди.  То же самое,
что pop в очереди и стеке

pop_back --- извлечь из конца очереди Задача

Реализовать двусвязный список. Сложность вставки  и удаления элемента в
конец должна быть О(1) Визуализация списка со сторожевыми элементами

head 16 11 8 21 tail

                      на
                     пут
                     в IT и

Вставка элемента head.next

head 16 11 8 21 tail

          9
       new_node

head.next.prev

              head.next

head 9 16 11 8 21 tail new_node Вставка элемента class Deque { class
Node {

      constructor() {
                                            constructor(data=null) {

          this.head = Node()

          this.tail = Node()
                                        this.data = data

          # при инициализации head указывает на tail
                this.next = null

          this.head.next = this.tail
                                this.prev = null

          # tail ссылается на head

          this.tail.prev =this.head
                              }

          # размер очереди при её создании равен 0
             }
          this.size = 0

      }

      function push_front(value) {

          # создать новый узел

          new_node = Node(value)

          # настроить его ссылки на

          # некогда первый элемент
                                        head.next
          new_node.next = this.head.next

          # предыдущий на head

          new_node.prev = this.head

                                                         head                16           11   8   21   tail

 

          # некогда первый элемент настроить на новый

          this.head.next.prev = new_node

          # head настроить на новый

          this.head.next = new_node
                               9
          this.size += 1
                                       new_node
      }

} head.next.prev Удаление элемента «Отцепить» элемент от списка
Уменьшить значение счётчика на единицу, то есть декрементировать его
tail как предпоследний элемент (prev) должен ссылаться на следующий до
предпоследнего элемент (tail.prev.prev) next должен ссылаться на tail
pop() Если head как next имеет tail --- список пуст pop_result.prev
pop_result pop_result.next

head 9 16 11 8 21 tail

                                                                                          pop_result.prev.next

Удаление элемента function pop_back() {

if this.head.next == this.tail {

       return null

}

\# извлекаем всегда только из начала очереди

    pop_result = this.tail.prev
                                                                              pop()

\# теперь tail в качестве prev (первого элемента очереди)

#ссылается на второй элемент pop_result.prev pop_result pop_result.next
this.tail.prev = pop_result.prev

\# переписываем next у второго элемента.  head 9 16 11 8 21 tail \#
Теперь next ссылается на tail pop_result.prev.next pop_result.prev.next
= pop_result.next

\# "отцепляем" наш элемент от списка

pop_result.next = null

pop_result.prev = null

\# уменьшаем счётчик

this.size -= 1

                                                                                             ill g
                                                                                                s...

return pop_result.data

                                                                                           sk in

}

                                                                                        IT ad
                                                                                          lo

Пример использования дека deque = Deque()

deque.push_back(7)

deque.push_back(8)

deque.push_back(9)

deque.push_back(11)

deque.push_front(16) head 21 16 7 8 9 11 tail

deque.push_front(21)

print deque // \[ 21 16 7 8 9 11 \]  push_front() push_back() print
deque.pop_back() // 11 

print deque.pop_back() // 9

print deque.pop_back() // 8

print deque.pop_back() // 7

print deque.pop_back() // 16 ид у в IT ›› print deque.pop_back() // 21

print deque // \[ \]  / C++ std::deque

Задача: получить доступ к произвольному элементу за О(1) C++ std::deque
Константный размер фрагмента важен, поскольку тогда можно легко
сопоставить индекс массива  с индексом внутри фрагмента за O(1).
Частично shift = 3

пустыми могут быть только первый и последний chunk_size = 6 фрагменты 0
Всякий раз, когда происходит push_back(), запись добавляется внутрь
последнего фрагмента или 1 выделяется новый фрагмент. Та же процедура
происходит в push_front() в первом фрагменте 2

Не стоит использовать метод insert() в середину 3 std::deque. Тогда
придётся совершить большое количество сдвигов 4

                                                      5

                                                          Допустимая реализация sit::deque

Резюме Очередь, стек и дек могут быть реализованы разными способами со
своими плюсами  и минусами Стек (LIFO) позволяет за О(1) вставить
элемент в конец и получить его первым Очередь (FIFO) позволяет за О(1)
вставить элемент в конец и получить его последним  в порядке очереди
Дек --- двунаправленная очередь, объединяет в себе преимущества стека и
очереди Спасибо  за внимание 
